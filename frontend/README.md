Frontend (Next.js)

Minimal scaffold for the LEGO Lookalike frontend.

Commands:

- Install dependencies: `npm install`
- Run dev server: `npm run dev`

This folder contains a tiny placeholder app and a `Webcam` component stub.
